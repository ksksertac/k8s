apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: testapi-ingress
  namespace: default
  annotations:
    kubernetes.io/ingress.class: "traefik"                # K3s Traefik Ingress Controller
    cert-manager.io/cluster-issuer: "letsencrypt-prod"    # TLS sertifikası için
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    traefik.ingress.kubernetes.io/router.tls: "true"
spec:
  tls:
  - hosts:
      - k3s.jrkripto.vip           # 🔥 Artık doğru domain
    secretName: testapi-tls
  rules:
  - host: k3s.jrkripto.vip         # 🔥 Burada da aynı domain
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: testapi
            port:
              number: 8080
