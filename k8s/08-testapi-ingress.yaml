apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: testapi-ingress
  namespace: default
  annotations:
    kubernetes.io/ingress.class: "traefik"          # K3s içindeki Traefik yönlendirici
    cert-manager.io/cluster-issuer: "letsencrypt-prod"  # TLS için
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    traefik.ingress.kubernetes.io/router.tls: "true"
spec:
  tls:
  - hosts:
      - api.jrkripto.vip
    secretName: testapi-tls
  rules:
  - host: api.jrkripto.vip
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: testapi
            port:
              number: 8080
