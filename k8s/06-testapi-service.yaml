apiVersion: v1
kind: Service
metadata:
  name: testapi
  namespace: default
spec:
  selector:
    app: testapi
  type: ClusterIP   # 🔥 Burayı değiştirdik
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP